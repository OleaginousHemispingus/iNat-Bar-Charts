# iNat Bar Charts
A python script that uses the iNaturalist API to create Bar Charts for a given taxon, like eBird does.

The script takes in a taxon, then finds the species of that taxon in a given area. For example, if you see a fishing spider, you might wonder which species of fishing spider are most common where you are. This script will be able to answer that.
